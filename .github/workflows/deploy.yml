# Cloudflare Pages se deploya automaticamente al hacer push a main.
# Este workflow no es necesario para Cloudflare Pages.
# Si usas GitHub Pages en el futuro, descomenta y ajusta.

# name: Deploy
# on:
#   push:
#     branches: [main]
# jobs:
#   deploy:
#     runs-on: ubuntu-latest
#     steps:
#       - uses: actions/checkout@v4
#       - uses: actions/setup-node@v4
#         with:
#           node-version: '18'
#       - run: npm install
#       - run: npm run build
#       - uses: actions/deploy-pages@v4
#         with:
#           artifact_name: github-pages
#           folder: ./dist
